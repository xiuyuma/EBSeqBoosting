#pragma once

#include <Eigen/Dense>
#include <vector>
#include <algorithm>
#include "helper.hpp"

struct aggregate
{
    using COUNTS = Eigen::MatrixXi;
    
    static COUNTS aggregate(COUNTS& counts, CLUSINFO clusInfo)
    {
        
    }
};
